server:
  port: 8060

spring:
  profiles:
    default: local

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: oracle.jdbc.OracleDriver

  jackson:
    serialization:
      INDENT_OUTPUT: true

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: eunnnnn1996@gmail.com
    password: juwpvmxbqjufyjbt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

